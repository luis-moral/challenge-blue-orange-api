spring:
  application:
    name: challenge-blue-orange-api
  main:
    banner-mode: "OFF"

server:
  address: 0.0.0.0
  port: 8080

endpoint:
  base-path: /api
  creators:
    path: ${endpoint.base-path}/creators
  creator-note:
    path: ${endpoint.base-path}/creator/note
  health:
    path: /status

redis:
  host: "localhost"
  port: 12305

marvel:
  api:
    base-url: https://gateway.marvel.com
    key: abcdefg12345