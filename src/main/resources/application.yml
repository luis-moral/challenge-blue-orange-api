spring:
  application:
    name: challenge-blue-orange-api
  main:
    banner-mode: "OFF"

server:
  address: 0.0.0.0
  port: 8080

redis:
  host: "localhost"
  port: 12305

endpoint:
  base-path: /api
  creator:
    path: ${endpoint.base-path}/creator
  health:
    path: /status

marvel:
  api:
    host: https://gateway.marvel.com
    port: 443
    key: abcdefg12345